<!DOCTYPE html>
<html lang="ja">
<head>
	<meta charset="utf-8">
</head>
<body>
<%= erb :header %>
<ul>
	<% Array(@busstops).each do |busstop| %>
		<div class="busstop" data-token="<%= csrf_token%>">
			<a href="/busstop/<%= busstop.id %>"><span class="busstoplist"><%= h(busstop.name) %></span><br><%= h(busstop.pre) %> - <%= h(busstop.muni) %></a>
		</div>
	<% end %>
</ul>
</body>
</html>
